@model VirtualClassroom.Core.TblUsers

@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>Register</title>

    <link rel="stylesheet" href="~/css/register.css" asp-append-version="true" />
    <!-- ✅ Validation Scripts (move here) -->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>
</head>

<body>

<div class="main-container">

    <!-- LEFT PANEL -->
    <div class="left-panel">
       
    </div>

    <!-- RIGHT PANEL -->
    <div class="right-panel">
        <div class="login-container">
                <div class="logo">
                    <img src="~/images/vclass.png" />
                </div>
            <h2>Create Account 🚀</h2>
            <p class="subtitle">Register to get started</p>

            <form asp-action="Register" method="post">

                <!-- Validation -->
                    <!-- REMOVE THIS -->
                    <div asp-validation-summary="ModelOnly"></div>

                <!-- Full Name -->
                <input asp-for="FullName" placeholder="Username" required />
                <span asp-validation-for="FullName" class="text-danger"></span>

                <!-- Email -->
                <input asp-for="Email" type="email" placeholder="Email Address" required />
                <span asp-validation-for="Email" class="text-danger"></span>

                <!-- Password -->
                <div class="password-wrapper">
                    <input asp-for="PasswordHash" type="password" id="password" placeholder="Password" required />
                    <span class="toggle-password" onclick="togglePassword()">👁</span>
                </div>
                
                <span asp-validation-for="PasswordHash" class="text-danger"></span>

                <!-- Role -->
                <select asp-for="Role" required>
                    <option value="">Select Role</option>
                    <option value="1">Faculty</option>
                    <option value="2">Student</option>
                </select>
                <span asp-validation-for="Role" class="text-danger"></span>

                <!-- Error -->
                @if (ViewBag.Error != null)
                {
                    <p class="error">@ViewBag.Error</p>
                }

                <button type="submit" class="login-btn">Register</button>

                <p class="or">or</p>

                <!-- Google Login -->
                <button type="button" class="google-btn"
                        onclick="location.href='@Url.Action("GoogleLogin","Account")'">
                    <img src="https://cdn.jsdelivr.net/gh/devicons/devicon/icons/google/google-original.svg"
                         class="google-icon" />
                    Continue with Google
                </button>

                <p class="register-link">
                    Already have an account?
                    <a href="/Account/Login">Login</a>
                </p>
            </form>
        </div>
    </div>

</div>
</body>
<script>
    function togglePassword() {
        const pass = document.getElementById("password");
        pass.type = pass.type === "password" ? "text" : "password";
    }
</script>
</html>

@* @section Scripts { *@
@*     <partial name="_ValidationScriptsPartial" /> *@
@* } *@