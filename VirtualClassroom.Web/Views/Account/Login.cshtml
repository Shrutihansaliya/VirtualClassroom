




 @* new css *@

@{
    Layout = null;
}

<link rel="stylesheet" href="~/css/login.css" />

<div class="main-container">

    <!-- LEFT SIDE (Branding + Info) -->
    <div class="left-panel">
        <div class="brand">
            <h1>Virtual Classroom</h1>
            <p>Learn • Teach • Collaborate</p>
        </div>

        <div class="features">
            @* <h3>What you can do?</h3> *@
            <ul>
                <li>📚 Manage Classrooms</li>
                <li>📝 Assignments & Submissions</li>
                <li>🎥 Live Online Sessions</li>
                <li>👨‍🏫 Faculty & Student Portal</li>
                <li>🔐 Secure Login with Google</li>
            </ul>
        </div>
    </div>

    <!-- RIGHT SIDE (LOGIN FORM) -->
    <div class="right-panel">
        <div class="login-container">
            <h2>Welcome Back 👋</h2>
            <p class="subtitle">Login to continue</p>

            <form method="post">
                <input type="email" name="email" placeholder="Email Address" required />
                <input type="password" name="password" placeholder="Password" required />



                <div class="forgot-container">
                    <a href="@Url.Action("ForgotPassword","Account")" class="forgot-link">
                        Forgot password?
                    </a>
                </div>

                <button type="submit" class="login-btn">Log In</button>

                <p class="or">or</p>

                <button type="button" class="google-btn"
                        onclick="location.href='@Url.Action("GoogleLogin","Account")'">
                    <img src="https://cdn.jsdelivr.net/gh/devicons/devicon/icons/google/google-original.svg"
                         class="google-icon" />
                    Continue with Google
                </button>

                <p class="register-link">
                    Don't have an account?
                    <a href="/Account/Register">Create Account</a>
                </p>

                @* <div class="forgot-password"> *@
                @*     <a href="@Url.Action("ForgotPassword", "Account")"> *@
                @*         Forgot Password? *@
                @*     </a> *@
                @* </div> *@

            </form>

            @if (ViewBag.Error != null)
            {
                <p class="error">@ViewBag.Error</p>
            }
        </div>
    </div>

</div>
